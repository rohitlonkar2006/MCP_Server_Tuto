[project]
name = "mcp-server-tuto"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=4.0.3",
    "langchain-mcp-adapters>=0.3.1",
    "mcp>=2.0.0",
    "rohits-agentic-terminal==0.1.0",
]
